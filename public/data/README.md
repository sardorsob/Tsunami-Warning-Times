# Generated browser data

The Vite build copies this directory to `app/dist/data/`. Scientific web assets
will be generated here by the accepted data pipeline and must not be hand-edited.
Other files in this directory are ignored until a task explicitly curates them.
