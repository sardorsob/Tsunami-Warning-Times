# Acquisition run notes

## Decision

Only contracts marked `approved` were eligible for retrieval; blocked contracts were recorded without network access.

## Limitations

Source-specific limits, missingness, datum, CRS, and temporal-semantics constraints remain in `inputs.json`; cached assets have no new response headers.
